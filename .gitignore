# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# Env / secrets
.env
.env.local
*.key

# Data / caches (point-in-time cache is regenerable; keep store/runs JSON committed though)
.cache/
data_cache/
# The SQLite index is a regenerable view over the git-tracked per-run JSON; don't commit the binary.
store/bear.sqlite
*.sqlite-wal
*.sqlite-shm

# Node / Next.js (web/)
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
